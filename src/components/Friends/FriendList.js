export const FriendList = (friends) => {
    return (
        
        {
            friends.map(friend => (
            
        )) }
    )
}

{/* <ul class="friend-list">
  <!-- Довільна кіл-сть FriendListItem -->
</ul> */}